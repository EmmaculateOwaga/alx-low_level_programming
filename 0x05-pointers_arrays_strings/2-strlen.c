#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: string
 * Return: returns the length as integer
 */

int _strlen(char *s)
{
	int len = o;

	while (*(s + len) != '\0')
		len++;

	return (len);
}
